# AI 주제 후보

마음에 드는 주제를 댓글로 선택하면 선택한 주제만 일반 초안 PR을 생성합니다.

예시:

```text
pick 2,3
```

후보 파일: `candidate-topics/20260716-1526-tech-blog-topic.json`

## 선택할 주제

- [ ] 1. PostgreSQL 연결 풀 고갈 문제를 로그·쿼리로 진단하고 해결하는 실무 가이드
- [ ] 2. Docker 컨테이너가 OOM으로 죽을 때 원인 분석과 메모리 제한 최적화 절차
- [ ] 3. systemd 서비스가 자동 재시작을 반복할 때 로그와 유닛 설정으로 원인 찾는 순서
- [ ] 4. CI 파이프라인에서 캐시로 잘못된 아티팩트가 복원될 때 원인 추적과 안전한 캐시 전략
- [ ] 5. 서버 TLS 핸드셰이크 오류가 발생할 때 인증서·체인·프로토콜·네트워크 순으로 점검하는 체크리스트
- [ ] 6. 비결정적(flaky) 테스트가 반복될 때 원인 분류와 재현 가능한 테스트로 만드는 실무 절차
- [ ] 7. 분산 트레이싱에서 샘플링 때문에 특정 요청이 보이지 않을 때 조사와 샘플링 설정 조정 방법
- [ ] 8. SPA에서 클라이언트와 서버의 폼 유효성 검사를 중복 없이 일관되게 설계하는 실전 전략
- [ ] 9. 마이크로서비스에서 비원자적 작업을 보장하기 위한 보상 트랜잭션(Saga) 설계와 실패 처리 패턴
- [ ] 10. 기술 블로그의 코드 샘플을 지속적으로 실행 가능하게 유지하는 버전·환경 명시 템플릿

## 후보 상세

### 1. PostgreSQL 연결 풀 고갈 문제를 로그·쿼리로 진단하고 해결하는 실무 가이드

- slug: `postgres-connection-pool-exhaustion-diagnosis`
- 추천 이유: 운영에서 접속 수 급증이나 누수로 서비스 응답이 느려질 때 빠르게 원인을 좁혀 복구해야 하기 때문입니다.
- 검색 의도: 커넥션 풀 고갈 원인(대기 큐, 장시간 쿼리, 풀설정)을 로그와 쿼리로 어떻게 파악하고 풀링 전략을 조정하는가
- 실무 가치: 실서비스 장애 복구와 커넥션 풀 튜닝으로 DB 가용성과 응답성을 즉시 개선할 수 있습니다.
- 최신성 포인트: PostgreSQL 및 커넥션 풀러(예: pgbouncer) 설정과 추천값은 버전별로 달라질 수 있어 최신 문서 확인이 필요합니다.
- 확인 권장 출처: PostgreSQL 공식 문서, pgbouncer/pgpool 문서, DBMS 릴리즈 노트
- 썸네일: Postgres 연결 풀 진단 / 로그·쿼리·설정으로 고갈 원인 찾기

### 2. Docker 컨테이너가 OOM으로 죽을 때 원인 분석과 메모리 제한 최적화 절차

- slug: `docker-container-oom-diagnosis-memory-limits`
- 추천 이유: 컨테이너 OOM은 서비스 불안정으로 이어지므로 재현법과 조치 절차를 정리해 두면 장애 대응 시간이 단축됩니다.
- 검색 의도: 컨테이너 OOM 시 cgroup, 로그, 앱 메모리 프로파일링으로 원인을 어떻게 좁히고 limit를 안전하게 설정하는가
- 실무 가치: 서비스 안정성 향상과 불필요한 재시작을 줄여 운영 비용과 장애 시간을 감소시킵니다.
- 최신성 포인트: Docker, runc, 커널 cgroup 동작과 메모리 제어 정책은 커널/런타임 업데이트에 따라 동작이 달라질 수 있습니다.
- 확인 권장 출처: Docker 문서, Linux cgroups 문서, 컨테이너 런타임 릴리즈 노트
- 썸네일: Docker OOM 대응 / 메모리 사용 분석과 limit 조정 절차

### 3. systemd 서비스가 자동 재시작을 반복할 때 로그와 유닛 설정으로 원인 찾는 순서

- slug: `systemd-restart-loop-debug-unit-config`
- 추천 이유: 서버에서 서비스가 재시작 루프에 빠지면 빠르게 정상 상태로 복구하기 위한 점검 순서가 필요합니다.
- 검색 의도: 재시작 루프의 원인이 유닛 설정, ExecStart 실패, 헬스체크, 또는 리소스 제약인지 어떻게 구분하고 수정하는가
- 실무 가치: 운영 중단을 최소화하고 재발 방지를 위한 유닛 설정과 로깅 개선 조치를 바로 적용할 수 있습니다.
- 최신성 포인트: systemd의 옵션과 journald 로깅 행동은 배포판과 버전별 차이가 있어 최신 매뉴얼 확인이 필요합니다.
- 확인 권장 출처: systemd 공식 매뉴얼(man pages), 배포판 운영 가이드, 서비스 벤더 문서
- 썸네일: systemd 재시작 루프 해결 / 저널로그·유닛 옵션으로 원인 추적

### 4. CI 파이프라인에서 캐시로 잘못된 아티팩트가 복원될 때 원인 추적과 안전한 캐시 전략

- slug: `ci-cache-restore-wrong-artifacts-debug-strategy`
- 추천 이유: 빌드나 테스트가 이전 아티팩트에 의존해 실패하면 배포가 막히므로 캐시 전략을 명확히 해야 합니다.
- 검색 의도: 캐시 키 설계, 경로 분리, 무효화 방법으로 잘못된 아티팩트 복원을 어떻게 추적하고 예방할 것인가
- 실무 가치: CI 신뢰성 향상과 불필요한 디버깅 시간을 줄여 개발 생산성을 개선합니다.
- 최신성 포인트: CI 도구(GitHub Actions, GitLab 등)의 캐시 구현과 최적 권장 설정은 서비스 업데이트로 변경될 수 있습니다.
- 확인 권장 출처: CI 벤더 문서(예: GitHub Actions, GitLab CI), 빌드 도구 문서
- 썸네일: CI 캐시 복원 문제 / 해시·키·분리 전략으로 오염 방지

### 5. 서버 TLS 핸드셰이크 오류가 발생할 때 인증서·체인·프로토콜·네트워크 순으로 점검하는 체크리스트

- slug: `tls-handshake-failure-checklist-server`
- 추천 이유: TLS 오류는 보안과 연결성에 직접 영향을 주므로 빠르게 원인을 좁혀야 합니다.
- 검색 의도: 인증서 체인, SNI, 프로토콜/암호화 스위트, 중간 장비(프록시/로드밸런서) 문제를 어떻게 확인하는가
- 실무 가치: 안전한 HTTPS 연결 복구와 클라이언트 호환성 확보로 서비스 접근성을 회복할 수 있습니다.
- 최신성 포인트: TLS 권장 설정과 약한 암호화 폐기 목록은 보안 권고와 라이브러리 업데이트에 따라 바뀝니다.
- 확인 권장 출처: RFC 문서, OpenSSL/라이브러리 문서, Mozilla Server Side TLS 가이드, 로드밸런서 벤더 문서
- 썸네일: TLS 핸드셰이크 디버그 / 인증서부터 네트워크까지 점검 체크리스트

### 6. 비결정적(flaky) 테스트가 반복될 때 원인 분류와 재현 가능한 테스트로 만드는 실무 절차

- slug: `flaky-test-investigation-isolation-fixes`
- 추천 이유: 테스트 신뢰도가 낮으면 CI 효율이 떨어지므로 플래키 테스트를 체계적으로 제거해야 합니다.
- 검색 의도: 환경 의존성, 시간 동기화, 병렬성, 외부 서비스 의존 등 플래키 원인을 어떻게 재현하고 격리하는가
- 실무 가치: 테스트 신뢰성 회복으로 CI 성공률 향상과 디버깅 비용 절감 효과를 얻을 수 있습니다.
- 최신성 포인트: 테스트 프레임워크와 모킹/시뮬레이션 기법은 도구 업데이트로 권장 방법이 바뀔 수 있습니다.
- 확인 권장 출처: 테스트 프레임워크 문서(pytest, JUnit, Jest 등), CI 벤더 가이드
- 썸네일: Flaky 테스트 제거 / 원인 분류·격리·영구 해결 절차

### 7. 분산 트레이싱에서 샘플링 때문에 특정 요청이 보이지 않을 때 조사와 샘플링 설정 조정 방법

- slug: `distributed-tracing-missing-spans-sampling-debug`
- 추천 이유: 중요한 요청이 추적되지 않으면 인시던트 원인 분석이 어려워져 트레이싱 설정을 점검해야 합니다.
- 검색 의도: 샘플링 규칙, 헤더 전파, SDK 구성, 에이전트 수집 한계로 인해 스팬이 누락되는 원인을 어떻게 찾고 조정하는가
- 실무 가치: 핵심 트랜잭션 가시성을 확보해 문제 원인 분석 시간을 단축할 수 있습니다.
- 최신성 포인트: OpenTelemetry 및 벤더별 에이전트의 샘플링 API와 권장 설정은 빠르게 발전하고 있습니다.
- 확인 권장 출처: OpenTelemetry 문서, 트레이싱 벤더(예: Jaeger, Zipkin) 문서, APM 벤더 가이드
- 썸네일: 분산 트레이싱 샘플링 / 누락 스팬 조사와 샘플링 정책 조정

### 8. SPA에서 클라이언트와 서버의 폼 유효성 검사를 중복 없이 일관되게 설계하는 실전 전략

- slug: `spa-client-server-form-validation-strategy`
- 추천 이유: UX를 해치지 않으면서 보안(서버 검증)을 유지하려면 검증 책임과 재사용 전략이 필요합니다.
- 검색 의도: 어떤 검증을 클라이언트에서 즉시 처리하고 어떤 검증을 서버로 위임해야 하며, 규칙을 공유하는 방법은 무엇인가
- 실무 가치: 개발 생산성 향상과 입력 기반 취약점 예방, 유지보수성 개선 효과를 제공합니다.
- 최신성 포인트: 프론트엔드 프레임워크와 Form 라이브러리의 기능 및 best practice는 빠르게 변합니다.
- 확인 권장 출처: React/Vue 공식 문서, HTML 폼 표준, 보안 가이드(OWASP)
- 썸네일: 폼 검증 일관화 / 클라이언트·서버 중복 최소화 전략

### 9. 마이크로서비스에서 비원자적 작업을 보장하기 위한 보상 트랜잭션(Saga) 설계와 실패 처리 패턴

- slug: `microservice-saga-compensating-transactions-design`
- 추천 이유: 분산 시스템에서 전통적 트랜잭션이 불가능할 때 일관성을 유지하는 실무 패턴이 필요하기 때문입니다.
- 검색 의도: 로컬 트랜잭션 실패 시 보상 액션 설계, 커뮤니케이션 방식(동기/비동기), 장애 시 복구 전략은 어떻게 구성하는가
- 실무 가치: 데이터 일관성 문제를 줄이고 서비스 간 실패 상황에서도 복구 가능한 아키텍처를 설계할 수 있습니다.
- 최신성 포인트: 메시징 시스템, 트랜잭션 지원 라이브러리, 클라우드 서비스의 기능 변화로 패턴 구현 선택지가 달라질 수 있습니다.
- 확인 권장 출처: 분산 시스템 패턴 문서, 메시지 브로커 벤더 문서, 클라우드 아키텍처 가이드
- 썸네일: Saga 보상 트랜잭션 / 마이크로서비스 일관성 유지 패턴

### 10. 기술 블로그의 코드 샘플을 지속적으로 실행 가능하게 유지하는 버전·환경 명시 템플릿

- slug: `blog-code-sample-runtime-version-template`
- 추천 이유: 독자가 바로 따라 해볼 수 있어야 게시물 가치가 유지되므로 실행 환경 명시는 필수입니다.
- 검색 의도: 코드 샘플에 어떤 런타임/라이브러리 버전과 실행 방법을 명시하면 재현성이 높아지는가
- 실무 가치: 독자와 팀 내부의 재현성 문제를 줄이고 포스트 유지보수 비용을 절감합니다.
- 최신성 포인트: 언어 런타임과 라이브러리 버전은 빠르게 업데이트되므로 템플릿과 권장 표기법을 주기적으로 점검해야 합니다.
- 확인 권장 출처: 언어/프레임워크 공식 문서, 패키지 매니저 문서, 블로그 플랫폼 가이드
- 썸네일: 블로그 코드 샘플 유지관리 / 버전·런타임 명시 템플릿

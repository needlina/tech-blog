# AI 주제 후보

마음에 드는 주제를 댓글로 선택하면 선택한 주제만 일반 초안 PR을 생성합니다.

예시:

```text
pick 2,3
```

후보 파일: `candidate-topics/20260716-1220-tech-blog-topic.json`

## 선택할 주제

- [ ] 1. Docker 빌드 캐시가 무효화되어 빌드 시간이 급증할 때 원인과 즉시 해결 방법
- [ ] 2. PostgreSQL 파티셔닝으로 대용량 테이블 조회 성능을 단계별로 개선하는 방법
- [ ] 3. 로그 샘플링으로 애플리케이션 로그 비용과 노이즈를 줄이는 실무 전략
- [ ] 4. TLS 인증서 자동 갱신이 실패했을 때 점검 순서와 무중단 롤백 방법
- [ ] 5. CI 파이프라인에서 비결정적(Flaky) 테스트를 분리하고 안정화하는 구체적 절차
- [ ] 6. Linux 시스템에서 디스크 I/O 병목을 진단하고 임시 완화책을 적용하는 절차
- [ ] 7. 클라우드 네트워크 ACL과 보안 그룹 충돌로 서비스 연결 장애가 발생했을 때 원인 분석 순서
- [ ] 8. 프론트엔드 이미지 지연 로딩으로 발생한 레이아웃 시프트를 줄이는 실전 기법
- [ ] 9. 마이크로서비스에서 SAGA 패턴으로 분산 트랜잭션을 단계적으로 도입하는 실무 가이드
- [ ] 10. 블로그 엔진에서 서버사이드 렌더링으로 메타 태그와 캐시를 자동화해 SEO를 안정화하는 체크리스트

## 후보 상세

### 1. Docker 빌드 캐시가 무효화되어 빌드 시간이 급증할 때 원인과 즉시 해결 방법

- slug: `docker-build-cache-invalidations-fix`
- 추천 이유: 빌드 시간이 CI 파이프라인 전체를 늦출 수 있어 원인 파악과 즉시 복구 방법이 곧 비용 절감으로 연결되기 때문이다.
- 검색 의도: 어떤 변경이 캐시를 무효화했는지 찾아서 캐시를 복구하거나 빌드 전략을 수정하는 방법을 알고 싶다.
- 실무 가치: CI 시간 단축, 비용 절감, 빈번한 배포에서의 생산성 회복에 바로 적용할 수 있다.
- 최신성 포인트: Docker와 BuildKit의 캐시 동작과 캐시 관련 옵션은 버전별로 동작이 달라 최신 도구 버전에 맞춰 확인해야 한다.
- 확인 권장 출처: Docker 공식 문서, BuildKit 릴리스 노트, CI 도구(예: GitHub Actions, GitLab CI) 문서
- 썸네일: 빌드 캐시 복구 / Docker/BuildKit 캐시 문제 진단과 복구

### 2. PostgreSQL 파티셔닝으로 대용량 테이블 조회 성능을 단계별로 개선하는 방법

- slug: `postgresql-partitioning-step-by-step`
- 추천 이유: 단일 대용량 테이블의 성능 이슈는 파티셔닝으로 해결 가능한 경우가 많고 안전한 이관 절차가 필요하기 때문이다.
- 검색 의도: 파티셔닝 종류 선택, 데이터 마이그레이션, 인덱스·쿼리 재검토 순서를 실제로 적용 가능한 단계로 알고 싶다.
- 실무 가치: 쿼리 응답 시간 개선과 운영 중단을 최소화하는 마이그레이션 계획을 실무에 바로 적용할 수 있다.
- 최신성 포인트: PostgreSQL의 파티셔닝 기능과 성능 최적화 옵션은 버전별로 개선되므로 대상 버전에 맞게 검증해야 한다.
- 확인 권장 출처: PostgreSQL 공식 문서, 릴리스 노트, 벤더 또는 클라우드 DB 마이그레이션 가이드
- 썸네일: 파티셔닝 단계 가이드 / 설계, 마이그레이션, 인덱스·쿼리 튜닝 순서

### 3. 로그 샘플링으로 애플리케이션 로그 비용과 노이즈를 줄이는 실무 전략

- slug: `log-sampling-reduce-cost-noise`
- 추천 이유: 로그 볼륨이 비용과 분석 효율을 동시에 악화시키므로 샘플링으로 핵심 정보를 유지하며 비용을 줄일 수 있다.
- 검색 의도: 어떤 기준으로 샘플을 수집하고, 중요한 이벤트를 놓치지 않으면서 로그 비용을 줄이는 정책을 만들고 싶다.
- 실무 가치: 로그 저장·처리 비용 절감과 SRE/개발자의 디버깅 생산성 향상에 즉시 도움이 된다.
- 최신성 포인트: 로그 수집/전송 에이전트와 관찰성 도구의 기능이 자주 업데이트되므로 구현 전에 지원 여부를 확인해야 한다.
- 확인 권장 출처: ELK/Elastic, Datadog, Prometheus/OTel 문서, 로그 에이전트(Fluentd, Fluent Bit) 가이드
- 썸네일: 로그 샘플링 전략 / 샘플링 정책 설계와 구현 체크리스트

### 4. TLS 인증서 자동 갱신이 실패했을 때 점검 순서와 무중단 롤백 방법

- slug: `tls-cert-auto-renew-failure-checklist`
- 추천 이유: 인증서 만료는 서비스 접속 불가로 직결되므로 자동화 실패 시 빠르게 복구하는 절차가 필요하다.
- 검색 의도: 자동 갱신 실패 원인을 단계적으로 점검하고, 서비스 중단 없이 이전 인증서로 되돌리는 방법을 찾고 싶다.
- 실무 가치: 서비스 가용성 보장과 인증서 관리 자동화 안정성을 실무에서 바로 향상시킬 수 있다.
- 최신성 포인트: 인증서 발급기관, ACME 클라이언트, 로드밸런서/인그레스 동작이 자주 변경되므로 구성별 최신 문서를 확인해야 한다.
- 확인 권장 출처: Let's Encrypt/ACME 문서, Certbot/ACME 클라이언트 릴리스 노트, 클라우드 로드밸런서 벤더 문서
- 썸네일: 인증서 자동 갱신 체크리스트 / 점검 항목과 무중단 롤백 절차

### 5. CI 파이프라인에서 비결정적(Flaky) 테스트를 분리하고 안정화하는 구체적 절차

- slug: `ci-flaky-test-isolation-and-fix`
- 추천 이유: Flaky 테스트는 CI 신뢰도를 떨어뜨려 배포 속도를 저하시켜 우선적으로 체계화해 관리해야 한다.
- 검색 의도: Flaky 테스트를 자동으로 분리하고 재현 환경을 구성해 근본 원인을 고치는 절차를 알고 싶다.
- 실무 가치: 테스트 안정성 향상으로 배포 속도와 개발자 신뢰도를 높이는 데 바로 적용할 수 있다.
- 최신성 포인트: 테스트 프레임워크와 CI 도구의 재현/격리 기능이 지속적으로 개선되므로 최신 도구 지원을 확인해야 한다.
- 확인 권장 출처: JUnit/Mocha 등 테스트 프레임워크 문서, CI 도구(GitHub Actions, Jenkins) 가이드, 테스팅 도구 릴리스 노트
- 썸네일: Flaky 테스트 관리 / 분리, 재현, 근본 원인 해결 단계

### 6. Linux 시스템에서 디스크 I/O 병목을 진단하고 임시 완화책을 적용하는 절차

- slug: `linux-disk-io-bottleneck-diagnosis-mitigation`
- 추천 이유: 디스크 I/O 병목은 애플리케이션 전반 성능 저하로 이어지므로 빠른 진단과 임시 완화가 필요하다.
- 검색 의도: iostat, blktrace, perf 등 도구로 병목 위치를 찾고 임시로 완화하는 설정과 조치를 알고 싶다.
- 실무 가치: 운영 중인 서버에서 다운타임을 최소화하며 성능을 부분 회복시키는 데 직접 적용할 수 있다.
- 최신성 포인트: 커널 버전과 스토리지 드라이버의 변화가 진단 결과와 권장 조치에 영향을 주므로 환경별 확인이 필요하다.
- 확인 권장 출처: Linux man 페이지, kernel changelog, 스토리지 벤더 성능 가이드
- 썸네일: 디스크 I/O 진단 / 진단 툴 사용법과 즉시 적용 가능한 완화책

### 7. 클라우드 네트워크 ACL과 보안 그룹 충돌로 서비스 연결 장애가 발생했을 때 원인 분석 순서

- slug: `cloud-acl-security-group-conflict-debug`
- 추천 이유: 네트워크 접근 제어 규칙 간 충돌은 서비스 장애로 이어지므로 빠르게 규칙 원인을 찾아 수정해야 한다.
- 검색 의도: 보안 그룹과 네트워크 ACL이 서로 다른 레벨에서 트래픽을 차단할 때 어떤 순서로 확인하고 고치는지 알고 싶다.
- 실무 가치: 접속 장애를 신속히 해결하고 잘못된 규칙을 재설계해 재발을 방지하는 데 바로 활용할 수 있다.
- 최신성 포인트: 클라우드 공급자별 네트워크 모델과 기능이 다르고 자주 업데이트되므로 벤더 문서로 현행 동작을 확인해야 한다.
- 확인 권장 출처: AWS/Azure/GCP 네트워킹 문서, VPC/서브넷/라우팅 공식 가이드, 클라우드 보안 베스트 프랙티스
- 썸네일: ACL·보안그룹 충돌 디버깅 / 점검 순서와 우회 또는 수정 전략

### 8. 프론트엔드 이미지 지연 로딩으로 발생한 레이아웃 시프트를 줄이는 실전 기법

- slug: `frontend-lazy-loading-reduce-layout-shift`
- 추천 이유: 이미지 lazy loading은 성능에 유리하지만 CLS 같은 사용자 경험 문제를 유발하므로 구체적 해결책이 필요하다.
- 검색 의도: 어떤 HTML/CSS/JS 패턴으로 레이아웃 시프트를 방지하고 이미지 로딩 성능을 유지하는지 알고 싶다.
- 실무 가치: 웹페이지 사용자 경험 개선과 Core Web Vitals 점수 향상을 위해 즉시 적용 가능한 기법을 제공한다.
- 최신성 포인트: 브라우저의 lazy-loading, prefetch, aspect-ratio 지원 상태가 지속적으로 개선되므로 대상 브라우저 호환성을 확인해야 한다.
- 확인 권장 출처: W3C 사양, 브라우저 벤더 문서(Chrome, Firefox), Core Web Vitals 가이드
- 썸네일: 이미지 지연 로딩 최적화 / placeholder, aspect-ratio, preloading 기법

### 9. 마이크로서비스에서 SAGA 패턴으로 분산 트랜잭션을 단계적으로 도입하는 실무 가이드

- slug: `microservices-saga-pattern-rollout-guide`
- 추천 이유: 분산 트랜잭션을 잘못 도입하면 데이터 불일치 위험이 커지므로 점진적 도입과 보상 설계가 필수적이다.
- 검색 의도: SAGA 설계 유형을 선택하고 기존 서비스와 충돌 없이 단계적으로 전환하는 방법을 알고 싶다.
- 실무 가치: 데이터 일관성을 유지하면서 마이크로서비스 구조로 이전하는 과정에서 바로 적용 가능한 실행 계획을 제공한다.
- 최신성 포인트: 분산 트랜잭션 패턴과 메시징 플랫폼의 기능이 발전하므로 채택 전에 현재 플랫폼 지원 수준을 확인해야 한다.
- 확인 권장 출처: 데이터베이스 트랜잭션 표준 자료, 마이크로서비스 설계 문서, 메시징 플랫폼( Kafka, RabbitMQ ) 벤더 가이드
- 썸네일: SAGA 단계적 도입 / 보상 트랜잭션 설계와 마이그레이션 체크포인트

### 10. 블로그 엔진에서 서버사이드 렌더링으로 메타 태그와 캐시를 자동화해 SEO를 안정화하는 체크리스트

- slug: `blog-ssr-meta-cache-seo-automation`
- 추천 이유: 블로그의 SEO와 검색 스니펫 안정성은 트래픽에 직접 연결되므로 SSR과 메타 자동화로 일관성을 확보해야 한다.
- 검색 의도: 서버사이드 렌더링에서 동적 메타 태그, 캐시 무효화, sitemap 자동화를 어떻게 구현할지 알고 싶다.
- 실무 가치: 검색 가시성 안정화와 유지보수 비용 감소를 위한 실제 체크리스트로 바로 적용할 수 있다.
- 최신성 포인트: 검색 엔진의 크롤링 동작과 프레임워크 SSR 지원 방식이 변할 수 있으므로 최신 권장사항을 확인해야 한다.
- 확인 권장 출처: Google Search Central 문서, 프레임워크(Next.js, Nuxt) 가이드, HTTP 캐싱 표준
- 썸네일: 블로그 SSR 자동화 / 메타 관리·캐시 전략·배포 체크리스트

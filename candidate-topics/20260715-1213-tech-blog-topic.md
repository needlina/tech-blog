# AI 주제 후보

마음에 드는 주제를 댓글로 선택하면 선택한 주제만 일반 초안 PR을 생성합니다.

예시:

```text
pick 2,3
```

후보 파일: `candidate-topics/20260715-1213-tech-blog-topic.json`

## 선택할 주제

- [ ] 1. PostgreSQL 파티셔닝으로 대용량 테이블의 삭제·보관 작업을 빠르게 만드는 실무 절차
- [ ] 2. Docker 볼륨과 호스트 파일 권한 충돌로 컨테이너가 쓰기 실패할 때 단계별 해결법
- [ ] 3. OpenTelemetry로 스팬과 로그를 연계해 복합 요청의 지연 원인을 찾는 방법
- [ ] 4. CI에서 병렬 통합 테스트를 돌릴 때 데이터베이스 격리와 시드 전략 설계
- [ ] 5. SPA에서 액세스 토큰 만료 시 무중단 리프레시와 실패 복구 UX 설계
- [ ] 6. 클라우드 IAM 정책을 서비스별로 최소 권한으로 점진 적용하는 롤아웃 체크리스트
- [ ] 7. 서비스 간 mTLS 도입 시 인증서 관리와 롤오버를 단계별로 안전하게 실행하는 방법
- [ ] 8. 이벤트 기반 시스템에서 중복 이벤트를 허용하면서 일관성을 보장하는 실무 디자인 패턴
- [ ] 9. TLS(ACME) 인증서 자동 갱신 실패가 발생했을 때 신속 복구 및 예방 절차
- [ ] 10. 실무 기술 블로그에 배포 가능한 코드 예제와 빌드 산출물을 함께 제공하는 워크플로우

## 후보 상세

### 1. PostgreSQL 파티셔닝으로 대용량 테이블의 삭제·보관 작업을 빠르게 만드는 실무 절차

- slug: `postgresql-partitioning-fast-archival-cleanup`
- 추천 이유: 대용량 테이블에서 대량 삭제가 서비스 성능을 저하시킬 때 파티셔닝으로 운영 중단 없이 데이터 보관과 삭제를 해결하기 위해 권장합니다.
- 검색 의도: 어떤 파티셔닝 전략을 선택하고 기존 테이블을 안전하게 리파티셔닝하며 파티션 단위로 아카이브/삭제하는 구체적 절차를 알고 싶다
- 실무 가치: 쿼리 성능 저하와 롤백 위험을 줄이고 보관 정책을 파티션 단위로 자동화해 운영 비용과 다운타임을 줄입니다.
- 최신성 포인트: PostgreSQL 파티셔닝 기능과 파티션 교체/병합 관련 권장 방식이 버전별로 달라 최신 문서를 확인해야 합니다.
- 확인 권장 출처: PostgreSQL 공식 문서, 릴리즈 노트, 운영사례 블로그
- 썸네일: PostgreSQL 파티셔닝 실전 / 삭제·아카이브 작업을 파티션으로 안전하고 빠르게 처리하는 절차

### 2. Docker 볼륨과 호스트 파일 권한 충돌로 컨테이너가 쓰기 실패할 때 단계별 해결법

- slug: `docker-host-file-permissions-fix`
- 추천 이유: 빈번한 실무 장애 원인인 볼륨 권한 문제를 빠르게 진단하고 복구하는 체크리스트가 있어야 운영 복구 시간을 단축할 수 있습니다.
- 검색 의도: 호스트 소유자·UID 불일치, SELinux/ACL 문제, 파일시스템 마운트 옵션 등으로 인해 쓰기 실패가 발생했을 때 어떤 순서로 확인하고 해결할지 알고 싶다
- 실무 가치: 배포 중 컨테이너가 파일을 쓰지 못하는 문제를 신속히 해결해 서비스 가용성을 유지하고 재발 방지 조치를 도입할 수 있습니다.
- 최신성 포인트: Docker와 호스트 커널/SELinux 설정, 기본 UID 정책은 환경과 버전에 따라 다르므로 배포 환경에 맞춘 최신 확인이 필요합니다.
- 확인 권장 출처: Docker 공식 문서, 운영체제(예: systemd/SELinux) 매뉴얼, 파일시스템 벤더 문서
- 썸네일: Docker 권한 문제 해결 / 호스트와 컨테이너 간 볼륨 권한 충돌을 진단하고 복구하는 단계

### 3. OpenTelemetry로 스팬과 로그를 연계해 복합 요청의 지연 원인을 찾는 방법

- slug: `opentelemetry-link-spans-logs-troubleshoot`
- 추천 이유: 분산 시스템에서 지연 원인 추적은 로그만으로 어렵고 스팬 연계가 있을 때 문제 원인을 정확히 찾기 쉬워집니다.
- 검색 의도: 스팬과 로그를 동일 요청으로 연계하는 방법, context propagation, 로그-스팬 바인딩 전략과 쿼리 방법을 알고 싶다
- 실무 가치: 오탐을 줄이고 문제 파악 시간을 단축해 SLA 준수와 빠른 복구를 실현할 수 있습니다.
- 최신성 포인트: OpenTelemetry SDK와 백엔드(collector, exporter) 구현체가 계속 업데이트되므로 현재 버전의 통합 방법을 확인해야 합니다.
- 확인 권장 출처: OpenTelemetry 사양 및 SDK 문서, 관측성 백엔드(예: Jaeger, Tempo) 문서
- 썸네일: 스팬+로그 연계 추적 / 스팬과 로그를 연결해 멀티서비스 요청 지연 원인을 빠르게 좁히는 실무 가이드

### 4. CI에서 병렬 통합 테스트를 돌릴 때 데이터베이스 격리와 시드 전략 설계

- slug: `ci-parallel-integration-tests-db-isolation`
- 추천 이유: 병렬 CI에서 데이터 충돌로 인한 테스트 불안정을 해소하면 빌드 신뢰도가 올라가고 개발 속도가 개선됩니다.
- 검색 의도: 테스트별 DB 격리 방법(네임스페이스, 컨테이너, 임시 스키마), 시드 초기화와 병렬 시드 충돌 방지 전략을 찾고 싶다
- 실무 가치: 플랫폼에 맞는 병렬화 전략을 적용해 CI 시간을 단축하고 플akey 테스트를 줄일 수 있습니다.
- 최신성 포인트: CI 도구와 DB 컨테이너 이미지, 테스트 프레임워크 업데이트로 최적화 방법이 달라질 수 있어 최신 설정을 확인해야 합니다.
- 확인 권장 출처: CI/CD 도구 문서(GitLab, GitHub Actions), 데이터베이스 벤더 문서, 테스트 프레임워크 가이드
- 썸네일: 병렬 통합 테스트 DB 설계 / CI 병렬 실행을 위한 안전한 DB 격리와 일관된 시드 전략

### 5. SPA에서 액세스 토큰 만료 시 무중단 리프레시와 실패 복구 UX 설계

- slug: `spa-token-refresh-fallback-ux`
- 추천 이유: 잘못 설계된 리프레시는 사용자 경험을 해치고 중복 요청이나 보안 취약점을 만들기 때문에 실무에서 명확한 패턴이 필요합니다.
- 검색 의도: 토큰 자동 갱신 흐름, 백그라운드 재시도와 최종 실패 시 사용자 재인증 처리를 어떻게 설계할지 알고 싶다
- 실무 가치: 서비스 중단 없이 인증 상태를 유지하거나 안전하게 로그아웃 처리해 사용자 불편과 보안 리스크를 줄입니다.
- 최신성 포인트: 브라우저 보안 정책, SameSite, 인증 스토리지 권장사항이 변경될 수 있어 현재 베스트 프랙티스를 확인해야 합니다.
- 확인 권장 출처: OAuth2/OIDC 사양, 브라우저 보안 가이드(MDN), 인증 라이브러리 문서
- 썸네일: 토큰 리프레시 UX / 토큰 갱신 실패를 우아하게 처리하는 리프레시 및 재인증 전략

### 6. 클라우드 IAM 정책을 서비스별로 최소 권한으로 점진 적용하는 롤아웃 체크리스트

- slug: `cloud-iam-least-privilege-rollout-checklist`
- 추천 이유: 권한 축소는 보안 강화에 필수지만 직접 적용 시 정상 동작을 깨뜨리기 쉬워 점진적 롤아웃이 필요합니다.
- 검색 의도: 서비스 영향 최소화와 되돌리기 계획을 포함해 IAM 정책을 안전하게 축소하고 검증하는 단계별 절차를 알고 싶다
- 실무 가치: 권한 오남용 위험을 줄이고 규정 준수를 지원하며 문제 발생 시 빠르게 롤백할 수 있습니다.
- 최신성 포인트: 클라우드 공급자별 IAM 기능과 정책 시뮬레이터/권장사항이 자주 업데이트되므로 최신 도구와 정책을 확인해야 합니다.
- 확인 권장 출처: 클라우드 벤더 IAM 문서(AWS, GCP, Azure), 보안 가이드라인, 릴리즈 노트
- 썸네일: IAM 최소권한 롤아웃 / 서비스별로 권한을 줄이는 단계와 검증 체크리스트

### 7. 서비스 간 mTLS 도입 시 인증서 관리와 롤오버를 단계별로 안전하게 실행하는 방법

- slug: `mtls-certificate-rollover-steps`
- 추천 이유: mTLS는 서비스 간 신뢰를 확보하지만 인증서 갱신과 롤오버 과정에서 통신 장애가 발생하기 쉬워 안전한 절차가 필요합니다.
- 검색 의도: 새 인증서 발급, 배포, 점진적 신뢰 전환, 실패 시 롤백 순서와 검증 항목을 알고 싶다
- 실무 가치: 무중단 인증서 교체로 보안 요구사항을 만족시키면서 서비스 가용성을 유지할 수 있습니다.
- 최신성 포인트: 인증서 포맷, 자동화 도구(Cert-Manager 등)와 보안 권장사항이 진화하므로 현재 환경에 맞춰 확인해야 합니다.
- 확인 권장 출처: mTLS 가이드, PKI/ACME 문서, 인증서 관리 툴 문서
- 썸네일: mTLS 인증서 롤오버 / 서비스 중단 없이 인증서를 교체하는 단계와 검증 절차

### 8. 이벤트 기반 시스템에서 중복 이벤트를 허용하면서 일관성을 보장하는 실무 디자인 패턴

- slug: `event-driven-deduplication-idempotency-patterns`
- 추천 이유: 네트워크 재시도나 중복 전송이 흔한 분산 시스템에서 일관성을 유지하는 구체적 패턴이 실무에 바로 도움이 됩니다.
- 검색 의도: 중복 이벤트 탐지, idempotency 키 설계, 상태 기반 머지(merge) 전략과 저장소 설계 방법을 알고 싶다
- 실무 가치: 데이터 불일치와 중복 작업을 줄여 운영 복잡성과 비용을 낮추고 장애 복구를 단순화합니다.
- 최신성 포인트: 메시지 브로커 기능과 저장소 트랜잭션 지원이 달라 패턴 적용 방식이 달라지므로 환경별 최신 확인이 필요합니다.
- 확인 권장 출처: 분산 시스템 설계 문서, 메시지 브로커(예: Kafka, RabbitMQ) 문서, 데이터베이스 트랜잭션 문서
- 썸네일: 이벤트 중복 처리 패턴 / 중복 허용 환경에서 일관성을 유지하는 idempotency·결과 합치기 전략

### 9. TLS(ACME) 인증서 자동 갱신 실패가 발생했을 때 신속 복구 및 예방 절차

- slug: `tls-acme-cert-renewal-failure-recovery`
- 추천 이유: 인증서 만료로 인한 서비스 단절은 심각하므로 자동갱신 실패 시 신속히 복구하고 재발을 막는 절차가 필요합니다.
- 검색 의도: 갱신 실패 원인 진단(도메인 검증, rate limit, DNS), 긴급 우회(임시 인증서)와 재발 방지 절차를 알고 싶다
- 실무 가치: 무단 서비스 차단을 방지하고 자동갱신 신뢰도를 높여 운영 부담을 줄입니다.
- 최신성 포인트: ACME와 인증서 발급자 정책, rate limit 규칙이 변경될 수 있어 현재 규약을 확인해야 합니다.
- 확인 권장 출처: ACME 사양, 인증서 발급자(예: Let's Encrypt) 문서, DNS/웹 서버 벤더 문서
- 썸네일: 인증서 자동갱신 복구 / ACME 자동갱신 실패 감지 후 우회·복구·예방 조치 체크리스트

### 10. 실무 기술 블로그에 배포 가능한 코드 예제와 빌드 산출물을 함께 제공하는 워크플로우

- slug: `blog-distributable-code-examples-workflow`
- 추천 이유: 독자가 바로 따라하고 검증할 수 있는 예제를 제공하면 블로그 신뢰도와 재사용성이 크게 향상됩니다.
- 검색 의도: 예제 코드의 빌드 산출물 생성, 버전 관리, CDN/패키지 배포와 블로그에 임베드하는 안전한 방법을 알고 싶다
- 실무 가치: 독자의 재현성을 높이고 유지보수가 쉬운 예제 배포 프로세스를 통해 기술 공유 효율을 개선합니다.
- 최신성 포인트: 패키지 저장소, CDN 정책, 저작권과 보안 권장사항이 변할 수 있어 배포 방식의 최신 규칙을 확인해야 합니다.
- 확인 권장 출처: CI/CD 도구 문서, 패키지 레지스트리 가이드(NPM, PyPI), CDN 및 저작권 가이드
- 썸네일: 블로그 코드 배포 워크플로우 / 재현 가능한 예제 제공을 위한 CI 빌드·패키징·배포 흐름

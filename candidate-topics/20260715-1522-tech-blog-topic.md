# AI 주제 후보

마음에 드는 주제를 댓글로 선택하면 선택한 주제만 일반 초안 PR을 생성합니다.

예시:

```text
pick 2,3
```

후보 파일: `candidate-topics/20260715-1522-tech-blog-topic.json`

## 선택할 주제

- [ ] 1. PostgreSQL에서 TOAST와 인덱스 bloat를 안전하게 줄이는 절차
- [ ] 2. Docker 컨테이너 로그 급증 원인별 로그 드라이버·회전 대응 방법
- [ ] 3. Linux에서 파일 디스크립터 부족으로 서비스가 죽을 때 진단과 긴급 복구 절차
- [ ] 4. CI에서 flaky 테스트를 자동으로 탐지하고 우선순위로 제거하는 실무 워크플로
- [ ] 5. TLS 인증서 체인·재협상 오류 발생 시 백엔드에서 점검할 우선순위 체크리스트
- [ ] 6. Docker Compose 환경에서 무중단 배포 및 롤백을 구현하는 단계별 패턴
- [ ] 7. 분산 트레이스에서 샘플링 편향이 서비스 성능 분석을 왜곡할 때 확인하고 보정하는 방법
- [ ] 8. 이미지 지연 로딩으로 CLS가 오히려 악화될 때 원인별 해결법
- [ ] 9. 데이터 마이그레이션 중 읽기·쓰기 일관성 보장을 위한 롤링 마이그레이션 패턴
- [ ] 10. 기술 블로그 글 발행 전 민감정보·라이선스·성능 검증을 위한 간단 체크리스트

## 후보 상세

### 1. PostgreSQL에서 TOAST와 인덱스 bloat를 안전하게 줄이는 절차

- slug: `postgresql-toast-index-bloat-cleanup`
- 추천 이유: 운영 데이터베이스에서 테이블과 인덱스가 비정상적으로 커질 때 서비스를 중단하지 않고 공간을 회복해야 하기 때문이다.
- 검색 의도: 어떤 쿼리와 툴로 bloat를 진단하고, online으로 안전하게 정리(예: pg_repack, VACUUM FULL 대체)하는 방법을 알고 싶다.
- 실무 가치: 디스크 사용량과 I/O를 줄여 성능을 회복하고 복구 시간과 비용을 낮출 수 있다.
- 최신성 포인트: 각 Postgres 버전별 통계 컬럼과 온라인 리팩터링 도구의 동작이 달라 최신 문서 검증이 필요하다.
- 확인 권장 출처: PostgreSQL 공식 문서, pg_repack/pg_reorg 릴리즈 노트, 벤더 백업·복구 가이드
- 썸네일: Postgres bloat 정리 / TOAST·인덱스 안전 축소 절차

### 2. Docker 컨테이너 로그 급증 원인별 로그 드라이버·회전 대응 방법

- slug: `docker-logs-spike-drivers-rotation-handling`
- 추천 이유: 로그 용량 급증은 디스크 고갈과 서비스 오류로 이어지므로 빠르게 원인을 좁혀 드라이버·회전 정책을 조정해야 한다.
- 검색 의도: json-file, journald, awslogs 등 드라이버별 로그 확장 원인과 즉시 적용 가능한 회전·집계 설정을 찾고 싶다.
- 실무 가치: 로그로 인한 장애와 비용을 줄이고 로그 집계 파이프라인 안정성을 확보할 수 있다.
- 최신성 포인트: Docker 엔진과 로그 수집기 업데이트로 드라이버 옵션과 퍼포먼스 특성이 바뀔 수 있어 최신 설정 확인이 필요하다.
- 확인 권장 출처: Docker 엔진 문서, 로그 드라이버 벤더 문서, 시스템d/journald 설명서
- 썸네일: 컨테이너 로그 관리 / 드라이버 선택과 로그 회전 대책

### 3. Linux에서 파일 디스크립터 부족으로 서비스가 죽을 때 진단과 긴급 복구 절차

- slug: `linux-fd-exhaustion-diagnose-recover`
- 추천 이유: FD 고갈은 갑작스런 연결 실패로 이어지므로 빠른 진단과 제한 완화가 필요하다.
- 검색 의도: ulimit, /proc, lsof, systemd 설정 등으로 어떤 프로세스가 FD를 소비하는지 확인하고 즉시 완화하는 방법을 알고 싶다.
- 실무 가치: 서비스 가용성을 회복하고 재발 방지를 위한 설정을 적용해 운영 리스크를 줄일 수 있다.
- 최신성 포인트: 커널 변수와 systemd 한계는 배포 버전마다 다르므로 환경에 맞춘 최신 값 검증이 필요하다.
- 확인 권장 출처: Linux 커널 문서, systemd 매뉴얼, lsof/ss 매뉴얼 페이지
- 썸네일: 파일 디스크립터 부족 / 진단·긴급 복구 체크리스트

### 4. CI에서 flaky 테스트를 자동으로 탐지하고 우선순위로 제거하는 실무 워크플로

- slug: `ci-flaky-test-detection-priority-workflow`
- 추천 이유: Flaky 테스트는 CI 신뢰도를 떨어뜨려 배포 절차를 지연시키므로 자동화로 빠르게 식별하고 조치해야 한다.
- 검색 의도: 재현 불가 테스트를 어떤 메트릭(플랫폼별 실패율, 시간대, 병렬성)로 잡아내고 우선순위화할지 알고 싶다.
- 실무 가치: 빌드 속도와 개발자 신뢰도를 개선하고 릴리즈 사이클을 안정화할 수 있다.
- 최신성 포인트: 테스트 러너와 CI 플랫폼의 병렬성·플랫폼별 실패 패턴이 변하므로 최근 CI 환경에 맞춰 설정을 점검해야 한다.
- 확인 권장 출처: CI 벤더 문서(GitHub Actions, GitLab CI, Jenkins), 테스트 프레임워크 공식 문서
- 썸네일: Flaky 테스트 관리 / 탐지·우선순위화 워크플로

### 5. TLS 인증서 체인·재협상 오류 발생 시 백엔드에서 점검할 우선순위 체크리스트

- slug: `tls-certificate-chain-renegotiation-troubleshoot`
- 추천 이유: TLS 문제는 연결 실패와 보안 취약으로 직결되므로 체인, SNI, 프로토콜 호환성 순으로 빠르게 점검해야 한다.
- 검색 의도: 체인 불일치, 중간 인증서 누락, SNI 설정, 프로토콜 재협상 이슈 등 어떤 항목을 어떤 도구로 확인할지 알고 싶다.
- 실무 가치: 서비스의 안전한 연결을 복구하고 클라이언트 호환성 문제를 최소화할 수 있다.
- 최신성 포인트: TLS/SSL 라이브러리와 CA 정책 변경으로 검증 방식과 권장 설정이 바뀔 수 있어 최신 표준 확인이 필요하다.
- 확인 권장 출처: OpenSSL 문서, RFC(예: TLS 사양), CA 발급 가이드, 웹서버 벤더 문서
- 썸네일: TLS 오류 점검 / 체인·재협상 우선 체크

### 6. Docker Compose 환경에서 무중단 배포 및 롤백을 구현하는 단계별 패턴

- slug: `docker-compose-zero-downtime-deploy-rollback`
- 추천 이유: 모든 환경이 Kubernetes로 전환되지 않으므로 Docker Compose 기반 서비스에서도 무중단 배포가 필요하다.
- 검색 의도: 로드밸런싱, 버전별 서비스 병렬 배포, 데이터 마이그레이션 안전 처리, 롤백 트리거 방법을 단계별로 알고 싶다.
- 실무 가치: 간단한 인프라에서 배포 실패 시 다운타임 없이 안전하게 이전 상태로 돌릴 수 있다.
- 최신성 포인트: Compose와 관련 툴(nginx, haproxy, swarm 등) 업데이트로 네트워크·볼륨 동작이 달라질 수 있어 최신 동작 확인이 필요하다.
- 확인 권장 출처: Docker Compose 문서, 로드밸런서 벤더 문서, 배포 자동화 도구 설명서
- 썸네일: Compose 무중단 배포 / 롤백 포함 단계별 패턴

### 7. 분산 트레이스에서 샘플링 편향이 서비스 성능 분석을 왜곡할 때 확인하고 보정하는 방법

- slug: `distributed-tracing-sampling-bias-detect-correct`
- 추천 이유: 샘플링 편향은 잘못된 성능 결론으로 이어지므로 편향을 진단하고 샘플링 전략을 보정해야 한다.
- 검색 의도: 어떤 메트릭으로 편향을 감지하고 reservoir, probabilistic, adaptive 샘플링을 어떻게 조정할지 알고 싶다.
- 실무 가치: 진단 정확도를 높여 실제 병목 지점을 빠르게 찾아내고 불필요한 최적화를 줄일 수 있다.
- 최신성 포인트: OpenTelemetry 등 트레이싱 표준과 에이전트 기능이 빠르게 발전하므로 최신 샘플링 기능을 확인해야 한다.
- 확인 권장 출처: OpenTelemetry 공식 문서, 트레이싱 벤더 가이드, 분산 추적 표준 문서
- 썸네일: 샘플링 편향 보정 / 분산 트레이스 신뢰도 향상

### 8. 이미지 지연 로딩으로 CLS가 오히려 악화될 때 원인별 해결법

- slug: `lazy-loading-images-cls-regression-fixes`
- 추천 이유: 잘못된 lazy-loading은 레이아웃 이동(CLS)을 유발하므로 원인에 맞는 구현 수정이 필요하다.
- 검색 의도: placeholder, width/height 미지정, aspect-ratio 사용, responsive srcset 등 어떤 변경으로 CLS를 줄일지 알고 싶다.
- 실무 가치: 사용자 경험과 검색 엔진 지표를 동시에 개선해 페이지 품질을 올릴 수 있다.
- 최신성 포인트: 브라우저의 lazy-loading 기본 동작과 웹 표준 속성이 업데이트될 수 있어 최신 권장 기법 검증이 필요하다.
- 확인 권장 출처: web.dev/Google Core Web Vitals 문서, 브라우저 개발자 문서, 이미지 최적화 가이드
- 썸네일: 이미지 지연 로딩과 CLS / 악화 원인별 수리법

### 9. 데이터 마이그레이션 중 읽기·쓰기 일관성 보장을 위한 롤링 마이그레이션 패턴

- slug: `rolling-data-migration-consistency-patterns`
- 추천 이유: 대규모 마이그레이션 중 데이터 불일치와 다운타임을 최소화하려면 일관성 패턴을 명확히 설계해야 한다.
- 검색 의도: dual-write, feature-flagged read path, backfill·compensating write 전략 등 어떤 순서로 마이그레이션을 진행할지 알고 싶다.
- 실무 가치: 서비스 중단 없이 스키마 변경과 데이터 이동을 안전하게 수행해 출시 리스크를 줄일 수 있다.
- 최신성 포인트: 데이터베이스 버전과 어플리케이션 프레임워크의 트랜잭션·복제 특성이 다르므로 환경별 최신 검증이 필요하다.
- 확인 권장 출처: 데이터베이스 벤더 문서(예: PostgreSQL, MySQL 복제 문서), ACID/분산 트랜잭션 표준 설명서
- 썸네일: 롤링 데이터 마이그레이션 / 읽기·쓰기 일관성 패턴

### 10. 기술 블로그 글 발행 전 민감정보·라이선스·성능 검증을 위한 간단 체크리스트

- slug: `tech-blog-publish-sensitive-license-performance-checklist`
- 추천 이유: 운영 경험을 공유할 때 실수로 민감정보를 노출하거나 라이선스 위반을 하는 사고를 미연에 방지해야 한다.
- 검색 의도: 발행 전 어떤 항목(로그·키·회사 내부 경로, 코드 라이선스, 벤치마크 재현성)을 검토해야 할지 구체적으로 알고 싶다.
- 실무 가치: 법적·보안 리스크를 줄이고 독자에게 신뢰성 있는 기술 콘텐츠를 안정적으로 제공할 수 있다.
- 최신성 포인트: 개인정보 보호 규정과 오픈소스 라이선스 해석 가이드가 변할 수 있어 최신 기준 확인이 필요하다.
- 확인 권장 출처: 회사 보안 정책 문서, 개인정보 보호 법령(또는 규정), 오픈소스 라이선스 가이드
- 썸네일: 블로그 출판 체크 / 민감정보·라이선스·성능 점검

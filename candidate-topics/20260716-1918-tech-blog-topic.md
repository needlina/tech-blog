# AI 주제 후보

마음에 드는 주제를 댓글로 선택하면 선택한 주제만 일반 초안 PR을 생성합니다.

예시:

```text
pick 2,3
```

후보 파일: `candidate-topics/20260716-1918-tech-blog-topic.json`

## 선택할 주제

- [ ] 1. PostgreSQL에서 대량 온라인 스키마 변경을 잠금 없이 안전하게 적용하는 절차
- [ ] 2. 컨테이너 OOM 원인 분석과 cgroup 메모리 설정으로 안정화하는 체크리스트
- [ ] 3. 마이크로서비스 간 로그와 트레이스를 일관되게 연관시키는 Trace ID 전파 설계
- [ ] 4. Kubernetes 환경에서 TLS 인증서 무중단 롤링 교체하는 단계별 절차
- [ ] 5. 초기 렌더를 차단하는 서드파티 스크립트를 안전하게 비동기/지연 로드하는 실전 패턴
- [ ] 6. CI 파이프라인에서 의존성 취약점 감지 후 우선순위 기반 자동 차단·임시 패치 워크플로 설계
- [ ] 7. 읽기-쓰기 분리 아키텍처에서 복제 지연과 일관성 문제를 테스트하고 완화하는 실전 체크리스트
- [ ] 8. 정적 사이트 생성기(Hugo 등)에서 Next.js 같은 SSR/ISR로 블로그 마이그레이션할 때 SEO와 링크를 유지하는 절차
- [ ] 9. 클라우드 로그 비용을 줄이는 핫·콜드 보관과 샘플링 파이프라인 설계 실무 가이드
- [ ] 10. 대형 프론트엔드 컴포넌트에 계약형 단위테스트와 시각적 회귀(Visual Regression)를 결합해 안정화하는 방법

## 후보 상세

### 1. PostgreSQL에서 대량 온라인 스키마 변경을 잠금 없이 안전하게 적용하는 절차

- slug: `postgres-online-schema-change`
- 추천 이유: 운영 중인 큰 테이블에 스키마 변경을 적용할 때 다운타임 없이 안정적으로 진행하는 방법을 실무 단계로 정리해 현장 적용성을 높입니다.
- 검색 의도: 대형 테이블의 컬럼 추가·타입 변경·인덱스 재구성 등을 온라인으로 안전하게 수행하는 방법과 사용 도구를 찾는다.
- 실무 가치: 데이터 마이그레이션 계획, 롤백 전략, 성능 영향 최소화 기법을 통해 배포 리스크를 줄이고 서비스 가용성을 유지할 수 있다.
- 최신성 포인트: Postgres 버전별 기능(예: concurrent index, partition improvements)과 도구 호환성이 자주 업데이트되어 최신 기준 확인이 필요하다.
- 확인 권장 출처: PostgreSQL 공식 문서, pg_repack/gh-ost/pt-online-schema-change 릴리즈 노트, 벤더 마이그레이션 가이드
- 썸네일: Postgres 온라인 스키마 변경 / 잠금 없이 컬럼/인덱스/타입 변경하기

### 2. 컨테이너 OOM 원인 분석과 cgroup 메모리 설정으로 안정화하는 체크리스트

- slug: `container-oom-analysis-cgroup-memory`
- 추천 이유: 운영 환경에서 컨테이너가 예기치 않게 OOM으로 재시작될 때 원인을 빠르게 좁히고 재발을 막는 실무 절차가 필요합니다.
- 검색 의도: 컨테이너 OOM 로그 해석, cgroup 메모리 한계 설정, OOM killer 동작을 조정하는 방법을 알고 싶다.
- 실무 가치: 메모리 제한/스왑 정책, JVM/런타임 메모리 조정, 노드 레벨 OOM 설정으로 안정적 서비스 운영에 기여한다.
- 최신성 포인트: 커널, 컨테이너 런타임, 오케스트레이터의 메모리 관리 동작이 변화하므로 환경별 최신 동작 확인이 필요하다.
- 확인 권장 출처: Linux kernel 문서, cgroups 공식 문서, Docker/Kubernetes 메모리 관리 가이드, 런타임(JVM/node) 메모리 설정 문서
- 썸네일: 컨테이너 OOM 분석 / cgroup과 OOM killer 튜닝 체크리스트

### 3. 마이크로서비스 간 로그와 트레이스를 일관되게 연관시키는 Trace ID 전파 설계

- slug: `trace-id-propagation-microservices`
- 추천 이유: 분산 시스템에서 문제 원인 분석을 빠르게 하기 위해 서비스 경계에서 일관된 식별자 전파와 상호 연계 규칙이 필요합니다.
- 검색 의도: HTTP/gRPC/메시지 큐 등 다양한 인터페이스에서 trace ID를 안전하고 효율적으로 전파하는 방법을 찾는다.
- 실무 가치: 로그와 트레이스를 연결하면 장애 탐지·상관분석·성능 튜닝 속도가 빨라지고 사고 대응 시간이 단축된다.
- 최신성 포인트: OpenTelemetry, W3C Trace Context 표준과 미들웨어 구현이 빠르게 발전하고 있어 최신 권장 방식을 확인해야 한다.
- 확인 권장 출처: OpenTelemetry 문서, W3C Trace Context 스펙, HTTP/gRPC 헤더 규약, 벤더별 APM/로그 설명서
- 썸네일: Trace ID 전파 설계 / 로그·트레이스 연관성 확보하기

### 4. Kubernetes 환경에서 TLS 인증서 무중단 롤링 교체하는 단계별 절차

- slug: `k8s-tls-certificate-rotation`
- 추천 이유: 만료 임박한 인증서를 교체할 때 서비스 중단 없이 신뢰 체인을 유지하는 구체적 절차가 운영자에게 필수입니다.
- 검색 의도: Ingress/Service/Secret/CSI 등 다양한 K8s 리소스에서 TLS를 안전하게 롤링 교체하는 방법을 알고 싶다.
- 실무 가치: 세션 유실 최소화, 클라이언트 호환성 검증, 자동 갱신(예: cert-manager)과의 통합으로 인증서 관련 사고를 방지한다.
- 최신성 포인트: cert-manager, Ingress 컨트롤러, 클라우드 로드밸런서 구현의 변화로 교체 절차와 자동화 방식이 달라질 수 있다.
- 확인 권장 출처: Kubernetes 공식 문서, cert-manager 가이드, 클라우드 벤더 로드밸런서/Ingress 문서, Let's Encrypt 문서
- 썸네일: K8s TLS 인증서 교체 / 무중단 롤링 교체 체크리스트

### 5. 초기 렌더를 차단하는 서드파티 스크립트를 안전하게 비동기/지연 로드하는 실전 패턴

- slug: `defer-third-party-scripts`
- 추천 이유: 광고·분석·위젯 같은 서드파티 스크립트가 페이지 성능을 해칠 때 사용자가 체감하는 지연을 줄이는 방법을 제시합니다.
- 검색 의도: 서드파티 스크립트를 안전하게 비동기 로드하거나 런타임에 삽입해 렌더 차단을 방지하는 구체적 기법을 찾는다.
- 실무 가치: 페이지 로드 성능 개선과 기능 보장을 동시에 달성해 사용자 경험과 핵심 지표를 개선할 수 있다.
- 최신성 포인트: 브라우저 스펙(loading, async, defer)과 서드파티 스크립트 제공 방식이 계속 변경되므로 최신 호환성 확인이 필요하다.
- 확인 권장 출처: MDN Web Docs, 서드파티 제공사 통합 가이드, 브라우저 릴리즈 노트
- 썸네일: 서드파티 스크립트 지연 로드 / 비동기화로 렌더 차단 줄이기

### 6. CI 파이프라인에서 의존성 취약점 감지 후 우선순위 기반 자동 차단·임시 패치 워크플로 설계

- slug: `ci-dep-vuln-blocking-fix-workflow`
- 추천 이유: 취약점이 CI에서 발견됐을 때 무작정 차단하거나 무시하는 대신 우선순위에 따라 자동화된 조치를 설계해야 위험을 줄일 수 있습니다.
- 검색 의도: 취약점 등급에 따른 빌드 실패 규칙, 임시 패치(오프라인 패치/포크), 책임자 알림 프로세스를 CI에서 자동화하는 방법을 알고 싶다.
- 실무 가치: 보안 관련 빌드 중단과 릴리스 지연을 균형있게 관리해 실제 서비스 위험을 낮출 수 있다.
- 최신성 포인트: 취약점 DB(CVE, NVD)와 패키지 매니저 취약점 스캐너의 규칙이 자주 업데이트되므로 연동 정책을 최신으로 유지해야 한다.
- 확인 권장 출처: OWASP 가이드, NVD/CVE 데이터, SCA 도구 문서(Snyk, Dependabot, GitHub Advisory), CI 도구 문서
- 썸네일: 의존성 취약점 자동화 워크플로 / 검출→차단→임시패치 흐름

### 7. 읽기-쓰기 분리 아키텍처에서 복제 지연과 일관성 문제를 테스트하고 완화하는 실전 체크리스트

- slug: `read-write-splitting-consistency-checklist`
- 추천 이유: 읽기 전용 복제본 사용 시 나타나는 읽기 지연으로 인한 일관성 문제를 재현하고 해결하는 방법이 실무에서 자주 필요합니다.
- 검색 의도: 복제 지연 시나리오를 재현하는 테스트, 세션 일관성·리디렉션 전략, 쓰기 후 읽기 강제 보장 패턴을 알고 싶다.
- 실무 가치: 사용자 경험 저하 없이 확장성을 확보하고 데이터 일관성을 보장하는 운영 규칙을 마련할 수 있다.
- 최신성 포인트: 데이터베이스 복제 방식과 클라이언트 라우팅 솔루션이 발전함에 따라 권장 패턴과 도구 호환성을 확인해야 한다.
- 확인 권장 출처: 데이터베이스 벤더 문서(PostgreSQL, MySQL), 오케스트레이션/프록시 문서(HAProxy, PgBouncer), 윤리적 재현용 테스트 가이드
- 썸네일: 읽기-쓰기 분리 일관성 / 복제 지연 테스트와 완화책

### 8. 정적 사이트 생성기(Hugo 등)에서 Next.js 같은 SSR/ISR로 블로그 마이그레이션할 때 SEO와 링크를 유지하는 절차

- slug: `blog-migration-ssg-to-ssr-seo-preserve`
- 추천 이유: 블로그 플랫폼을 바꿀 때 검색 순위와 기존 링크를 유지하는 것은 트래픽 손실을 막기 위해 필수적입니다.
- 검색 의도: URL 매핑·301 리디렉션, 메타·구조화 데이터 유지, 소셜 카드·검색 엔진 색인화 전략을 어떻게 적용할지 알고 싶다.
- 실무 가치: 검색 엔진 트래픽과 외부 링크 유입을 유지하면서 새로운 기능(SSR/ISR)을 도입할 수 있다.
- 최신성 포인트: 검색 엔진 색인화 동작과 프레임워크별 렌더링 옵션이 업데이트되므로 마이그레이션 시 최신 권장 설정을 확인해야 한다.
- 확인 권장 출처: Google Search Central, Next.js/Hugo 문서, 웹마스터 도구 가이드, CDN/호스팅 벤더 문서
- 썸네일: 블로그 SSG→SSR 마이그레이션 / SEO·링크 무손실 전환 절차

### 9. 클라우드 로그 비용을 줄이는 핫·콜드 보관과 샘플링 파이프라인 설계 실무 가이드

- slug: `cloud-log-cost-hot-cold-sampling`
- 추천 이유: 로그량이 늘면서 비용이 급증할 때, 서비스 가시성을 유지하면서 비용을 통제할 수 있는 구체적 설계가 필요합니다.
- 검색 의도: 핫/콜드 스토리지 분리, 지표 기반 샘플링, 지연 아카이빙·검색성 유지 방법을 찾는다.
- 실무 가치: 관찰성 목표를 보존하면서 로그 저장 비용을 크게 절감하고, 긴급 조사 시 복구 가능성을 보장한다.
- 최신성 포인트: 클라우드 로깅 요금제와 데이터 내보내기(API, 버킷) 정책이 자주 변경되므로 최신 가격·기능을 확인해야 한다.
- 확인 권장 출처: AWS/GCP/Azure 로그 서비스 문서, OpenTelemetry/Fluentd/Fluent Bit 가이드, 스토리지 벤더 비용 문서
- 썸네일: 클라우드 로그 비용 절감 / 핫/콜드 저장 및 샘플링 설계

### 10. 대형 프론트엔드 컴포넌트에 계약형 단위테스트와 시각적 회귀(Visual Regression)를 결합해 안정화하는 방법

- slug: `component-contract-visual-regression-testing`
- 추천 이유: UI 컴포넌트가 복잡해질수록 계약(입출력) 테스트와 시각적 회귀를 조합해 회귀를 조기에 발견해야 합니다.
- 검색 의도: 컴포넌트 API 계약 테스트(Prop/args/응답)와 시각적 차이 감지를 CI에서 효율적으로 결합하는 방법을 알고 싶다.
- 실무 가치: 디자인 회귀·인터랙션 버그를 사전에 잡아 프론트엔드 릴리스 품질을 높이고 롤백 비용을 줄인다.
- 최신성 포인트: 테스트 도구(Playwright, Cypress, Chromatic 등)와 CI 통합 방식이 발전하고 있어 최신 통합 방식을 확인해야 한다.
- 확인 권장 출처: 테스트 프레임워크 문서(Cypress, Playwright, Jest), 시각적 회귀 도구 문서, CI 제공자 가이드
- 썸네일: 컴포넌트 계약·시각적 테스트 / 단위와 스냅샷을 결합한 안정화
